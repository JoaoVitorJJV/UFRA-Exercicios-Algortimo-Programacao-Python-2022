# Faça um algoritmo para ler uma mensagem na tela e imprimir a mesma mensagem
# 10 vezes na tela

# Utilizaremos o loop for, com o range(), para que o loop seja executado 10 vezes

for i in range(10):
    print(f"Printando pela {i + 1}° vez!")